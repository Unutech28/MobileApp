import { RFValue } from "react-native-responsive-fontsize";
export default {
    container: {
        flexGrow: 1,
      },
      dropContainer: {
        padding: RFValue(5),
        flex: 0.25,
        zIndex: 20,
        alignItems: "flex-end",
      },
}