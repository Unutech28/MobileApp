import GLOBALS from '@constants';
const {COLOR} = GLOBALS;

export default{
    homeContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: COLOR.BACKGROUND,
      }, 
}